[project]
name = "parse-optimistic"
authors = ["Celestial Data Systems"]
description = "A decentralized, blockchain-native platform for recording and validating astronomical observations"
telemetry = false
requirements = []
[contracts.celestial-observer]
path = "contracts/celestial-observer.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
